@props(['id','type','name','cols'])

<textarea
id="{{ $id }}"
 type="{{ $type }}"
 name="{{ $name }}"
 cols="{{ $cols }}"
 rows="{{ $rows }}"
 class="form-control"
 id="{{ $id }}"
 placeholder="Enter {{ $name }}">
</textarea>
